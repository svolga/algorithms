package java.yandex.sprint1.task4;

public class Mark {
}
/*

        StringBuilder stringBuilder = new StringBuilder();
        char[] buf = new char[1024];
        int numRead = 0;
        try {
            while ((numRead = reader.read(buf)) != -1) {
                String readData = String.valueOf(buf, 0, numRead);
//            stringBuilder.append(String.valueOf(buf, 0, numRead));
            }
            reader.close();
        }
        catch(Exception e){
            e.printStackTrace();
        }
        finally {
            try {
                reader.close();
            } catch (IOException e) {
                e.printStackTrace();
            }
        }
*/

